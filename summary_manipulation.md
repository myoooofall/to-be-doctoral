# Manipulation 与 JEPA / 潜空间世界模型论文笔记

## JEPA 相关研究时间线（按首次公开时间）

这张表是**领域脉络索引**，不代表每篇都已在下文精读；后续新论文按时间插入。这里把「表征学习」「带动作的世界模型」「下游决策」分开，避免把所有方法都笼统称为 JEPA。日期取论文首次公开时间，发表状态需单独核对。

| 时间 | 工作 | 关键变化 | 与机器人决策的关系 |
|---|---|---|---|
| 2018-03 | [World Models](https://arxiv.org/abs/1803.10122) | 从经验学习潜空间动力学，再利用模型决策；**不是 JEPA**。 | 提供更早的世界模型背景；不能把世界模型视作近两年才出现。 |
| 2022-06 | [A Path Towards Autonomous Machine Intelligence](https://openreview.net/pdf/315d43ba26f55357a84cec9a7ed15a6610094f79.pdf) | LeCun 提出 JEPA 式「在表征空间预测」的研究蓝图。 | 概念框架，不是具体机器人算法。 |
| 2023-01 | [I-JEPA](https://openaccess.thecvf.com/content/CVPR2023/html/Assran_Self-Supervised_Learning_From_Images_With_a_Joint-Embedding_Predictive_Architecture_CVPR_2023_paper.html) | 用图像部分区域的表征预测另一部分表征，避免逐像素重建。 | 主要是视觉表征；没有动作条件动力学。 |
| 2024-02 | [V-JEPA](https://arxiv.org/abs/2404.08471) | 将特征预测推进到视频时空表征。 | 视频理解/预测；原版没有以机器人动作作为条件。 |
| 2025-02 | [PLDM](https://arxiv.org/abs/2502.14819) | 从无奖励的离线交互数据学动作条件潜空间动力学，并用于目标规划。 | 与 LeWM 同属潜空间规划对照，但并非 SIGReg 式 LeJEPA。 |
| 2025-06 | [V-JEPA 2 / V-JEPA 2-AC](https://arxiv.org/abs/2506.09985) | 先从大规模视频学习表征，再用机器人数据训练动作条件预测器。 | 从视频表征走向机器人规划；需区分无动作预训练与有动作适配两阶段。 |
| 2025-11 | [LeJEPA](https://arxiv.org/abs/2511.08544) | 提出 SIGReg 这一防表征坍塌的正则化方法。 | 训练目标/表征层面的进展，本身不是机械臂世界模型。 |
| 2026-03 | [LeWM](https://arxiv.org/abs/2603.19312) | 从像素端到端联合训练编码器与动作条件预测器，用 SIGReg 防坍塌；仿真中用 CEM 规划。 | 核心问题：能否稳定学出可规划的 latent。详见下文。 |
| 2026-08 | [PSG-JEPA](https://arxiv.org/abs/2608.06799) | 在 LeWM 上加入机器人静态状态与跨时刻关节变化监督，让 latent 更易读出控制相关量。 | 从「可预测」推进到「与物理状态对齐」；仿真和桌面双臂实物，详见下文。 |

**目前可见的主线**：图像/视频表征预测 → 动作条件的 latent 动力学与规划 → 端到端稳定训练 → 对 latent 加机器人状态约束。它不是严格单线继承：PLDM、V-JEPA 2 与 LeWM 的数据和技术选择不同，PSG-JEPA 才是直接以 LeWM 为骨干的后续工作。

## ACT
reference:Learning Fine-Grained Bimanual Manipulation with
Low-Cost Hardware（RSS 2024）

## LeWorldModel: Stable End-to-End Joint-Embedding Predictive Architecture from Pixels (LeWM)

- **作者/时间/状态**：Lucas Maes、Quentin Le Lidec 等；arXiv:2603.19312，2026-03 初稿，本文阅读的是 2026-06-03 v3。论文标注为 **preprint**，不能据此说已被某会议/期刊接收。
- **原文与代码**：[论文 PDF](https://arxiv.org/pdf/2603.19312) · [论文 HTML](https://arxiv.org/html/2603.19312) · [官方代码、数据和 checkpoint](https://github.com/lucas-maes/le-wm)。
- **一句话**：用离线的「图像、动作、下一图像」序列，训练一个**动作条件的潜空间世界模型**；测试时给当前图像与目标图像，利用 CEM 在模型中搜索连续动作序列，再在仿真环境中执行。创新主要是用简单的 SIGReg 防止端到端 JEPA 表征坍塌，而非提出新的机械臂/腿臂一体控制器。
- **相关性定位**：值得借鉴的是「无需像素重建与奖励标签，也能学可用于短程视觉规划的动力学表征」；**没有**验证四足移动操作、接触丰富的全身控制或真机迁移。

### 先把三个概念分开：JEPA ≠ 世界模型 ≠ 规划器

假设机械臂正在推一个 T 形物体。我们手里有当前相机图像，以及一张“物体已经被推到目标位置”的图片。我们想决定接下来怎样推动。本文不是直接学 `图片 → 动作`，而是分两件事：**先学「给定动作，局面会怎样变化」；再用这个预测能力寻找动作。**

| 名词 | 一句话意思 | 在本文里对应什么 |
|---|---|---|
| **JEPA**（Joint-Embedding Predictive Architecture）| 一种**训练/表示架构**：不预测下一张图片的每个像素，而预测下一张图片经过编码后的特征。 | 图像编码器 `E` + 特征预测器 `F`，用“预测特征 ≈ 实际下一帧的特征”来学习。 |
| **世界模型**（world model）| 一个能回答**「如果现在做动作 `a`，之后会是什么状态？」**的模型；可以理解为从数据学出的近似模拟器。 | 训练好的 `E` 和动作条件预测器 `F` 合起来：`z_{t+1} ≈ F(z_t, a_t)`。它预测的是未来的压缩状态，不是 RGB 图像。 |
| **CEM / MPC 规划器** | 模型训练完之后，利用它**试很多动作，再挑好动作**的决策方法。 | CEM 搜索动作序列，使模型预测的终点接近目标图像的特征；执行动作、获取新图像后可再规划。 |

三个概念**不是同义词**：JEPA 是“怎么学表征/预测”的方法；世界模型是学到的“动作→后果”预测能力；CEM/MPC 是“怎样使用这个预测能力来选动作”。JEPA 也可用于图像遮挡块预测，未必包含机器人动作或成为可控世界模型；世界模型也可以预测像素、显式物理状态，不一定用 JEPA。**LeWM 正好用 JEPA 方式训练了一个动作条件的世界模型，再接上 CEM 规划器。**

这里的 `z`（latent）是**一张观察图像的压缩表征**，不是前面 humanoid 文献中“可解码成关节动作的技能 latent”。`z` 不等于人工定义的物体坐标；它是神经网络从图像学出来的一串数字。训练时既没有给 `z` 标注“第几维是物体位置”，也没有要求预测器重建完整 RGB 图。

### 用一次「推 T 形物体」把整个方法走完

**第一阶段：收集已有行为，不训练控制策略。**数据中一条记录是：当前图片 `o_t`、当时实际执行的动作 `a_t`、动作后的图片 `o_{t+1}`。连续多条记录就是轨迹。PushT 使用现成专家轨迹；其他环境也用已有启发式/SAC 行为。这里的 `a_t` 是环境原有的连续动作，不是模型自己生成的“latent action”。训练**不需要奖励标签**，但**必须有动作记录**。

**第二阶段：训练模型预测「动作的后果」。**同一个图像编码器 `E` 把当前图像变成 `z_t=E(o_t)`，把真实的下一张图变成 `z_{t+1}=E(o_{t+1})`。预测器 `F` 只看当前 `z_t`、动作 `a_t`（实现上也可用短历史），给出预测 `z_pred=F(z_t,a_t)`。训练让 `z_pred` 尽量接近真实的 `z_{t+1}`。例如，如果动作让操作点向右移动，模型应预测相应的操作点/物体状态变化。`E` 与 `F` **同时训练**；目标特征也由正在训练的 `E` 算出，而不是来自一个预先冻结的大模型。

**这里会出现 JEPA 的核心问题：坍塌。**如果 `E` 无论看到什么都输出同一个常数，那么 `F` 永远预测这个常数就能获得零预测误差，但它其实什么都没学会。所以本文额外用 **SIGReg** 约束一批 `z` 的分布，避免它们全部相同。可以先把 SIGReg 理解为“强迫不同图像在特征空间中保持足够多样”；随机投影、高斯检验是其具体实现，不是理解主流程的前提。**预测特征 + 防坍塌**，就是 LeWM 的训练核心。

**第三阶段：训练结束，给它一个新目标图像。**编码当前图片得到 `z_now`，编码目标图片得到 `z_goal`。CEM 随机生成许多候选动作序列，比如“先向右推、再向上推”。对每条序列，反复调用已训练好的 `F`，在特征空间“想象”几步后的终点。哪个序列的预测终点最接近 `z_goal`，就优先执行哪个。动作并不是 `F` 直接输出的：`F` **只负责预测后果**，CEM **负责找动作**。在仿真环境中执行后再获得新图像、重新规划——这才构成完整控制闭环。

```text
训练：已有 (当前图像, 实际动作, 下一图像)
             ↓
      E 编码前后两张图；F 根据前图特征和动作预测后图特征
             ↓
      预测误差 + 防坍塌正则 → 得到视觉潜空间世界模型

测试：当前图像 + 目标图像 → E 编码
             ↓
      CEM 提出动作候选 → F 在 latent 中预测后果 → 选最接近目标者
             ↓
      在环境中执行 → 再拍图 → 需要时重新规划
```

因此，这篇**不是在线 RL，不训练一个直接 `observation → action` 的 actor**。论文说的“end-to-end”，是指 `E` 和 `F` 从像素联合训练，而不是说从相机到真实机器人电机的所有环节都端到端学习。之所以预测特征而不是完整 RGB，是为了避免在背景纹理、光照、反射等细节上花很多建模成本，让大量候选轨迹评估更轻；但**不重建像素并不自动保证特征保留速度、接触、物体姿态等控制必需信息**。

### 真正的任务、输入、动作与数据

训练阶段是**离线、无奖励标签**的世界模型学习：数据集包含图像序列及相应连续动作；不需要训练任务的 reward、显式物理状态或目标标注。轨迹来自启发式策略、专家或 SAC 策略，**不是无需收集行为数据**；论文也明确要求数据覆盖足够多的相关动力学。

测试阶段是**目标图像条件下的短程控制/规划**：给当前图像 `o_t` 和作为目标的图像 `o_g`，优化一段连续动作，使预测的最后一个 latent 靠近 `enc(o_g)`。动作直接采用各仿真环境原本的连续控制接口；**不是统一的 motor torque，也不是论文新发明的低层执行器分配**。不同 benchmark 的动作含义随环境而变。

| 仿真环境 | 实际任务 | 离线数据来源 | 结果及需要注意的事 |
|---|---|---|---|
| TwoRoom | 红点穿过门到另一房间；2D 导航 | 1 万条噪声启发式轨迹，平均 92 步 | LeWM **87%**，PLDM **100%**、DINO-WM **97%**；最简单环境反而落后。 |
| Reacher | 2 关节平面机械臂到目标构型/位置 | 1 万条 200 步轨迹，SAC 收集 | LeWM **86%**，PLDM **79%**、DINO-WM **78%**。 |
| PushT | 平面推 T 形物体到目标构型 | 2 万条专家轨迹，平均 196 步 | LeWM **96%**，PLDM **78%**、DINO-WM 加本体状态 **92%**。这是最强的操作结果，但仍是 2D 推物。 |
| OGBench-Cube | 3D 机械臂抓取并放置**单个**方块 | 1 万条 200 步轨迹，benchmark 启发式收集 | LeWM **74%**，PLDM **65%**，DINO-WM 纯视觉 **84%**、加本体状态 **86%**；LeWM 不是最优。 |

以上是论文 Fig. 6 的成功率，**均为仿真**。测试时从一条离线轨迹抽一个初始帧，再取同一条轨迹 **25 个环境步以后**的帧作目标；最多执行 **50 个环境步**。这保证目标可达且通常接近数据分布，不等于任意目标图像、长程任务或未知场景泛化。模型训练不看 reward，但成功率评估自然使用环境定义的成功条件。

### 方法细节：怎么训练、为什么不会「全部变成一样的特征」

视觉编码器为从零训练的 ViT-Tiny（约 5M 参数）；用 `[CLS]` token，经一层投影和 BatchNorm 得到约 **192 维**的观察 embedding。动作条件预测器是带 causal mask 的 Transformer（约 10M 参数），以 AdaLN 注入动作，可利用最近几帧历史，输出下一帧 latent；合计约 **15M 参数**。这与把 DINOv2 等大型视觉模型先预训练并冻结后再学动力学的 DINO-WM 不同。

核心目标可记为：

`L = || pred(enc(o_t), a_t) − enc(o_{t+1}) ||² + λ · SIGReg({enc(o)})`。

1. 第一项是**下一 latent 预测误差**。单独使用时有一个作弊解：编码器对任何图都输出同一常数，预测误差立刻很小，但无法规划。
2. 第二项 **SIGReg**（Sketched-Isotropic-Gaussian Regularizer）把一批 embedding 投到许多随机方向上，检查每个一维投影是否接近标准正态，以鼓励整体 latent 分布接近各向同性高斯。常数 embedding 不满足这个要求，因此可防止这种简单坍塌。默认 `M=1024` 个随机方向、`λ=0.1`；作者认为投影数不太敏感，主要需要调 `λ`。
3. 编码器与预测器共同反传，没有 target encoder 的 EMA、stop-gradient、像素重建 loss、reward prediction 或预训练视觉 encoder。训练图像为 `224×224`；相邻建模帧间跳过 5 个环境步，batch 128，各任务训练 10 epochs。

**别把防坍塌等同于学到了充分物理状态**：它只抑制「所有图同一个 `z`」这样的退化表示，不能保证隐变量一定保存接触模式、精细姿态、速度，或 Euclidean latent 距离一定等于任务价值。作者对高斯正则的理论动机和实验验证也不等于对真实机器人成功率的保证。

### 测试时怎么用世界模型：CEM / MPC 不是另训一个 actor

先算 `z_now = enc(o_now)` 和 `z_goal = enc(o_goal)`。CEM（交叉熵法）从一个动作序列分布采样候选，在冻结的预测器中逐步想象每条动作序列的潜状态，将终点代价设成 `|| z_pred_final − z_goal ||²`，挑最好的一批动作更新采样分布，重复后执行动作并用新观察重规划。这是**采样式轨迹优化**，无须规划器对动作可微；也没有任务 reward 网络。

附录的配置：每轮 **300** 条候选，保留最好 **30** 条；PushT 最多 **30** 轮迭代，其余环境 **10** 轮；规划 horizon 为 **5 个模型步 = 25 个环境步**（frame skip 为 5）。主文把它称为 MPC，附录具体写的是**执行完整个优化序列后再规划**，所以不要误读成每一底层控制步都重新闭环优化。规划越长，潜空间递推误差越容易积累。论文给出完整规划用时约 **0.98 s**，DINO-WM 约 **47 s**（该实验设置下约 48 倍）；这不表示真机上已有高频控制回路。

### 论文实验证明了什么、没证明什么

- **证实的较强点**：在 PushT、Reacher 上，以简单两项 loss 学出的纯视觉世界模型可以胜过文中 PLDM 和 DINO-WM 配置；较小 latent 大幅降低候选轨迹搜索成本。作者开源训练、评估代码、数据与 checkpoints，复现实验入口较明确。
- **并非全面 SOTA**：TwoRoom 落后，3D Cube 落后于 DINO-WM；Fig. 3 的「相同 FLOPs 下更好」与 Fig. 6 的常规成功率是两个不同的比较，不能混为一谈。DINO-WM 的原始方法可用本体状态，论文也分别给了带/不带 proprio 的版本，比较模态要看清。
- **latent probing**：用训练完成后另拟合的线性/MLP probe 检查 latent 能否预测位置、角度等。这是诊断，不是训练损失。PushT 的位置、物体角度较好；但 3D Cube 上 LeWM 的关节速度、末端 yaw、方块姿态/朝向恢复较差。作者为可视化额外训练的图像 decoder 也不是模型训练或规划所需部件，长程 rollout 中末端角度等细节会丢失。
- **所谓「物理理解」要限缩**：模型对物体瞬移有较大预测误差峰值，对颜色突变反应较小。这说明它在这些合成干预下更敏感于位置连续性；不能据此推断它理解摩擦、抓取接触或腿足地形力学。
- **没有做的事**：没有真机、移动底座与机械臂协同、腿足接触、复杂多物体操作、未见任务上的长时程规划或 sim-to-real。论文限制还包括离线数据覆盖要求、动作标注需求，以及低多样性数据下高斯正则可能不合适。

### 对我们可能做的 mani-loco 的启发（下面是研究设想，不是论文结果）

1. **最值得学的是训练机制，而非直接照搬输入/动作**：可以先收集 `(camera/depth, proprio, action, next observation)` 轨迹，学行动条件世界模型，再用它做视觉目标的局部规划或给 RL policy 提供辅助表征；但原文只从图像编码。四足/人形操作的 base 速度、关节速度、接触/力觉对动力学很关键，仅靠单帧视觉尤其不足；可显式融合本体历史并验证哪些量确实留在 latent。
2. **分层动作比直接搜全身关节命令更现实**：原文的 CEM 面对低维连续动作。若我们让 CEM 一次搜索全身几十自由度 × 多步，组合维度、采样成本和模型误差都会显著上升。更可行的试验是高层搜索少量「base 位移/速度、末端目标、抓手模式」或可执行技能码，低层用已有 loco/manipulation controller 执行；这需要另做接口设计与安全约束，论文没有给出解决方案。
3. **goal 与 cost 必须改成真正的操作需求**：原文仅比较目标图像和预测终点 embedding 距离。mani-loco 需要区分到达、避障、物体状态、接触成功、稳定性与能耗；背景/视角变化可能影响全局图像 latent。可以测试对象中心/空间分解表征，以及 task-weighted latent cost，但这些是后续假设。
4. **优先验证模型在关键变量上的充分性**：除了最终成功率，要探针或 rollout 评估 base velocity、EE 6D 位姿、物体 6D 位姿、接触事件、抓取成功、失稳前兆；重点关注原文已暴露的速度与姿态缺失。若潜空间能稳定预测位置，却保不住接触/姿态，高层规划会在最关键时刻失效。
5. **实验起点建议**：在一个「走到桌边→推动或拾取单物体」的短程仿真任务上，固定低层控制器，对照纯 BC / 现有 RL、冻结视觉 encoder 的 world model、LeWM 式端到端 encoder + SIGReg、以及加入 proprio/接触监督的扩展；控制数据量与规划预算，测 OOD 目标、视角/照明变化和长 horizon，再考虑真机。不能把论文 PushT 的 96% 直接外推到移动操作。

**我的判断**：这篇对于「世界模型表征怎么稳定训练」是有用的基线，尤其适合作为 mani-loco 高层、低维、短程视觉规划的启发；但原方法缺少全身动力学、接触细节与真机证据。我们若要做 mani-loco，真正的研究空白不只是把 JEPA 接到四足上，而是让表示、动作抽象、规划与低层执行一起覆盖 locomotion/manipulation 的耦合和不确定性。

## Is Forward Prediction Enough? Physical State Grounding for JEPA World Models（PSG-JEPA）

- **作者/时间/状态**：Haodong Yan 等；2026-08-07 arXiv v1，**预印本**，目前不能写成已被会议/期刊接收。
- **原文与代码**：[论文 PDF](https://arxiv.org/pdf/2608.06799) · [论文 HTML](https://arxiv.org/html/2608.06799) · [项目页](https://haodong-yan.github.io/psg-jepa-project-page/) · [官方代码](https://github.com/Haodong-Yan/PSG-JEPA)。
- **一句话**：沿用 LeWM 的视觉编码器、动作条件 latent 预测器和 SIGReg，在训练时再用机器人本体状态及**多时间跨度的关节角变化**监督 latent；检验这些 latent 是否更容易被下游规划器和策略使用。
- **任务定位**：研究核心是**世界模型表征学习**，不是新提出一个端到端移动操作控制器。实验包括仿真的目标条件机械臂任务、LIBERO 操作任务，以及桌面双臂实物操作；没有腿式底座或 mani-loco 实验。

### 为什么 LeWM 之后还要做这篇？

LeWM 的训练目标要求「给定当前图像 latent 和动作，可以预测未来图像 latent」，再用 SIGReg 防止所有图像映射成常数。但**预测准确**不保证单个 latent 能方便地读出关节角、抓手状态、末端朝向，也不保证一对 latent 能方便地读出机器人如何运动。如果这些量藏得很深，下游小规划器/策略仍需从演示数据里重新学习视觉与动作的对应关系。

PSG-JEPA 将这个缺口称为 **physical identifiability**，具体是「冻结 encoder 后，简单线性/MLP probe 能否从 latent 还原机器人状态或状态变化」。这是可测的**表征可读性**，并不等于模型已经理解摩擦、接触或完整物理定律。

### 方法流程：保留 JEPA 主干，只在训练时加两种物理监督

训练轨迹除了连续 RGB 观察 `o_t` 和实际动作 `a_t`，还需与图像对齐的机器人状态 `s_t`，其中包含**关节角、抓手状态、末端位姿**。用 `q_t` 特指状态中的关节角向量，避免和 LeWM 里的视觉 latent `z_t` 混淆。模型的实际视觉输入仍是图像：机器人状态用作**监督目标**，不是编码器或动作预测器的输入。

1. **原有前向预测**：`E(o_t) = z_t`；动作条件预测器 `F(z_t, a_t, 历史)` 预测下一帧 latent，目标是实际下一帧的 `E(o_{t+1})`。保留 LeWM 的前向预测 loss 与防坍塌的 SIGReg。文中使用 3 帧上下文、4 帧训练窗口。
2. **静态状态头**：`H_s(z_t) → ŝ_t`，通过 MSE 对齐真实 `s_t`。目的不是把本体数据直接喂给规划器，而是迫使**单帧 latent**包含易于读出的机器人构型、抓手、末端姿态等信息。
3. **动态变化头**：`H_Δ(z_i, z_{i+k}) → q_{i+k}−q_i`，对窗口内 `k=1,2,3` 的所有有效 latent 对做 MSE，每个 horizon 等权平均。它要求**两帧 latent 的差异**能解释关节角的净变化。训练的是关节位移，不是预测每一步动作、物体运动或接触力。
4. **联合优化**：`L = L_forward + 0.09·L_SIGReg + 0.1·(L_static + L_dynamic)`。两个辅助头只在训练时存在，训练结束后丢弃；保留下来的是被这些 loss 改造过的 `E` 与 `F`，下游评估使用 encoder 特征。

因此，这篇相对 LeWM 的方法增量确实主要是**两个辅助 loss**。但它改变了数据假设：原 LeWM 可只依赖图像和动作日志，这里还要有同步的关节/抓手/末端状态标签。它仍不需要 reward 标签，却不能再被描述为只用「图像+动作」的纯自监督学习。测试时不用额外物理状态标签，也没有两个训练头的推理开销；**这不等于下游策略完全零成本**，它仍需训练规划器或策略头。

### 为什么预测「关节变化」，而不只是预测动作？

作者加了一个 `LeWM_ActionIDM` 对照：从相邻 `(z_t,z_{t+1})` 反推执行过的动作 `a_t`。PSG-JEPA 认为，跨多个时间步时，即便起点和终点相同，也可能由多种动作序列达到；若让 latent 对反推动作序列，监督目标会有多解，而且序列长度随 horizon 变化。相比之下，两个已知端点的 `q_{t+k}−q_t` 是固定维度、由端点唯一决定的净变化，更适合作为**端点表征**的监督。

这个论证合理，但应限缩：相同的关节净变化也可能对应不同的运动路径、速度和接触过程。因此 `Δq` 对机器人构型变化有用，却不是完整动力学；尤其不能把它直接外推为足端接触/物体交互的充分描述。

### 实验怎么做，结果说明什么

| 层级 | 具体做法与代表结果 | 正确解读 |
|---|---|---|
| **latent probe** | 冻结 OGBench-Cube encoder，用简单 probe 恢复状态。末端 yaw 的线性/MLP Pearson `r`：LeWM **0.08/0.08**，PSG **0.94/0.98**；关节角 **0.71/0.69 → 0.83/0.81**。 | 对文中定义的机器人状态，PSG latent 明显更易读；但 probe 的可读性不等于控制成功。末端位置本来就已接近 `r≈0.99`，并非所有量都有巨大提升。 |
| **目标条件规划** | **冻结 encoder，另训 GC-IDM 目标条件逆动力学规划器**，而不是原 LeWM 的 CEM。在 OGBench-Cube，GC-IDM 仅训 5 epochs、用全部演示数据时：PSG **95.0±0.7%**，LeWM **80.7±1.9%**；训 100 epochs 时 **98.7±1.2%** 对 **89.7±0.2%**。 | 这里的「规划效率」主要指**下游规划器更容易训练、所需演示更少**，不是 CEM 搜索速度加快。不能把本实验的 95% 与 LeWM 原文 CEM 的 Cube 74% 当同协议横向比较。 |
| **仿真策略** | LIBERO-Goal 10 项操作任务，给视觉 encoder 接动作策略头并联合微调；PSG **85.3±3.9%**，LeWM **77.7±0.5%**，ActionIDM **82.6±2.2%**。 | 证明该预训练表征能帮助另一种策略学习；执行时并非依靠世界模型 `F` 在线想象/CEM，也不是零样本操控。 |
| **实物操作** | AgileX 双臂桌面平台，正面与腕部相机；3 个放置/倒水任务，各有 100 条遥操作示范用于学习、每任务 50 次评估。PSG 成功率 **84/74/80%**，LeWM **62/58/60%**。 | 有真实机器人验证，且差异不小；但这是**经任务演示训练后的桌面操作**，不是预训练模型直接零样本转真机，也不是机器人行走时操作。 |

论文还报告 Cube 上 latent 开环递推误差降低；这说明预测器也受益。不过不同方法的 latent 坐标和尺度并不完全相同，**跨模型直接比较绝对 latent MSE** 要谨慎。更硬的下游证据是统一 GC-IDM / 策略协议下的成功率，而不是单一潜空间误差数值。对照包括同骨干 LeWM、增加动作反推头的 LeWM_ActionIDM 和 DINOv2；额外状态监督是 PSG 的有利条件，比较应连同数据需求一起看。

### 消融：哪部分真正起作用？

在 Cube 规划器只训 5 epochs 时，完整版 **95.0±0.7%**；去掉**动态变化头**降到 **81.3±2.4%**，几乎回到 LeWM 的 **80.7±1.9%**；去掉**静态状态头**仍有 **93.3±1.2%**。因此，Cube 上最大的规划增益主要来自关节变化监督。相反，去掉静态头会明显削弱单帧状态 probe（平均线性 `r` 从 **0.94** 到 **0.69**）。

仅用相邻 horizon 或仅用窗口端点，规划成功率分别为 **93.5±1.9% / 93.7±2.0%**，与完整多 horizon 的 **95.0±0.7%** 相比增益不大；论文也承认部分消融间差异落在种子波动范围内。所以可以说**两种监督功能互补**，但不能断言「多 horizon 本身」已被非常强地独立证明。

### 对 mani-loco 的价值与边界

**值得借鉴的思路**是：别只问 latent 能不能预测未来，还要问它是否让控制真正需要的量「容易读出」，并把这些量放进辅助训练目标。若迁移到腿臂一体，可设想检验/监督 base 速度与位姿变化、足端接触、末端位姿、物体相对位姿，再比较这些量对规划与策略的因果贡献。这是我们的研究设想，**不是本文实验结果**。

**不能直接照搬的地方**是：`q_{t+k}−q_t` 只覆盖机械臂关节净位移，可能漏掉底座移动、足端打滑、接触事件、物体受力和碰撞；真实移动操作还涉及相机视角剧变和身体遮挡。论文的真机平台虽采用 Mobile ALOHA 式结构，展示的仍是桌面双臂任务，没有验证移动底座运动、更没有腿足控制。它的贡献应定位为**让 JEPA latent 更对齐机器人状态、改善下游样本/训练效率**，而非已经实现统一的视觉 world-model 移动操作。

## From Policy Actions to Whole-Body Dynamics: A Unified Projection-Based Framework for Versatile Legged Loco-Manipulation

- **作者/时间/发表状态**：Chengzhen Yan、Qingchen Liu、Jiahu Qin、Yiming Jiang、Yang Shi；IEEE Robotics and Automation Letters（RA-L），2026 年 9 月接收；[DOI: 10.1109/LRA.2026.3734869](https://doi.org/10.1109/LRA.2026.3734869)。
- **一句话**：冻结一个已有的 RL 腿部运动策略，将它的关节空间动作通过 **Policy Action Dynamics Projection（PADP）** 转成满足刚体动力学与足端接触约束的全身动力学先验，再由一个统一 QP 同时处理腿部运动、末端位姿和接触力任务，最终输出全身关节力矩。

### 研究问题：怎样复用一个现成的行走策略完成全身移动操作？

腿臂机器人进行 loco-manipulation 时，不能把“腿负责行走、机械臂负责操作”简单地看成两个互不相关的任务。例如：

- 末端目标超出机械臂自身工作空间时，底座需要主动移动、降低或抬高身体；
- 擦墙、拉拽和开门时，末端接触力会反作用到底座，腿部需要重新分配支撑力；
- 在不平地上操作时，控制器还要同时维持足端接触、平衡、执行器限制和末端精度。

作者要解决的核心问题是：**已经有一个能够稳定行走、适应地形的 RL locomotion policy，能否不重新训练它，就把它接入一个可显式处理末端位姿、接触力和物理约束的 whole-body controller？**

这篇论文因此不是研究“如何感知物体并规划一个完整操作任务”，而是研究：**上层已经给出末端运动或接触力命令之后，如何让腿、浮动底座和机械臂在动力学层协调执行。**

### 现有方法有什么缺陷？

#### 1. 端到端 whole-body RL：能够学协同，但任务和约束都绑在策略里

一种做法是从头训练一个统一策略，让它直接控制腿、底座和机械臂。这类方法能够产生自然的全身协同行为，但作者认为存在三个问题：

- 末端位置、姿态和接触力目标通常要通过 reward 或训练任务显式编码；
- 摩擦、力矩上限、接触一致性等物理约束很难像优化器一样直接、严格地施加；
- 更换任务目标、约束或交互优先级时，往往需要重新训练或大幅微调策略。

因此它不容易复用一个已经训练好的通用 locomotion policy，也不方便在部署后灵活切换自由空间跟踪、控力、擦墙等任务。

#### 2. Locomotion policy + 独立机械臂控制器：可以复用，但全身没有真正协调

另一种做法是让已有 RL policy 控制腿部行走，再给机械臂单独配置 Arm-QP 或 manipulation controller。问题是两个模块通常只通过速度命令、落足点或 residual 等较高层接口联系：

- 机械臂够不到目标时，底座不一定知道应该怎样移动或调整高度；
- 末端受到外力时，腿部支撑力和身体运动不能与机械臂统一优化；
- 手臂任务可能破坏原 locomotion policy 所依赖的身体状态分布。

这正是论文实验中 `RL + Arm-QP` 和 `PADP + Arm-QP` 两个解耦基线的主要不足。

#### 3. 纯 model-based whole-body MPC/QP：约束清楚，但缺少已有学习策略的运动能力

全身 MPC/QP 可以直接写入刚体动力学、接触、摩擦锥、力矩限制和末端任务，具有清楚的物理含义。但在复杂地形和快速运动中，设计稳定、灵活的 locomotion objective 并不简单；非线性 MPC 的计算成本也可能较高。

与此同时，RL locomotion policy 已经从大量仿真交互中学到了地形适应和稳定运动能力。如果完全舍弃它、重新依靠模型优化构造运动行为，就没有利用这部分已有能力。

### 作者的解决思路与贡献

论文的基本思路不是在上述三条路线中任选一条，而是组合它们各自的长处：

1. **保留并冻结已有 RL locomotion policy**，把它作为稳定、适应地形的运动先验；
2. 提出 **Policy Action Dynamics Projection（PADP）**，把 policy 的关节空间动作翻译成满足刚体动力学和足端接触条件的全身变量；
3. 再使用统一 whole-body QP，将这个 locomotion prior 与末端位置、姿态、力控制及物理约束放在同一个优化问题中；
4. 让 QP 优化出的底座运动反过来生成 policy 的运动命令，形成 policy 与全身控制器之间的闭环协调。

所以其贡献的准确定位是：提出一个**学习 locomotion 与 model-based whole-body control 之间的动力学接口及统一执行框架**。它没有学习 manipulation policy，也没有视觉、语言、抓取规划或物体识别；用户通过遥控器给定末端运动/姿态，控制器负责让底座、腿和机械臂协调完成命令。

### 为什么二者不能直接连接？缺少的正是动力学层接口

常见 RL locomotion policy 输出的是腿关节的期望位置或速度，随后由 PD 控制器变成关节力矩。可是 whole-body controller 通常在以下变量上优化：

`x = (q̈, F_c, τ)`，即**广义加速度、接触力/末端 wrench、关节力矩**。

因此两边的语言不同：policy 只说“腿关节应该去哪里”，全身 QP 却要知道“身体怎样加速、各足施加多大接触力、各关节输出多少力矩”。

PADP 的目的就是补上这个**动力学层接口**。这里的 “projection” 不是神经网络 latent、降维或动作投影到某个学习空间，而是一个有约束的二次优化：从所有动力学可行的全身状态变化中，寻找最接近原 policy 意图的解。

### 完整控制流程：两个优化阶段分别干什么

```text
本体状态 + 自适应 locomotion command
                ↓
冻结的 RL locomotion policy（50 Hz）
                ↓
腿关节期望位置/速度 → PD 计算 policy 隐含的腿部力矩 τ_loco^π
                ↓
PADP：在动力学、接触运动学、摩擦与力矩限制下求 x_PADP
                ↓
x_PADP 作为“尽量保留原 locomotion 行为”的软先验
                ↓
统一 whole-body QP：加入末端位姿、力/混合力位任务和全身约束（500 Hz）
                ↓
最终全身关节力矩 τ → 电机
                ↘
优化出的底座运动又生成下一轮 locomotion command
```

#### 1. 冻结的 locomotion policy 输出什么？

policy 在 Isaac Gym 中独立训练，运行时只提供 locomotion joint 的期望位置、期望速度；轮足平台还输出轮子的期望速度。它不输出末端目标、接触力或机械臂动作，也不因本文的操作任务而重新训练。

期望关节状态经 PD 关系转成 policy 隐含的腿部力矩：

`τ_loco^π = K_p(q_loco^des − S_loco q_j) + K_d(q̇_loco^des − S_loco q̇_j)`。

这里的 `τ_loco^π` 仍只是腿部 action 的力矩解释，并没有告诉控制器足端力和浮动底座加速度怎样配合。

#### 2. 第一阶段 PADP 到底求什么？

PADP 求解：

`x_PADP = argmin ||S_loco τ − τ_loco^π||²`

并同时满足：

- 完整刚体动力学 `M(q)q̈ + h(q,q̇) = Sᵀτ + J_cᵀF_c`；
- 活跃足端的接触加速度约束；点足保持接触点不动，轮足满足滚动运动学；
- 线性化摩擦锥、接触力、末端 wrench、关节力矩上下限，以及非接触足零接触力。

所以 `x_PADP` 是对同一份 policy 腿部力矩的一种**接触一致、动力学可行的分解**：它补出了可能的 `q̈、F_c、τ`。但不能把它说成恢复了 policy 内部“真实的 whole-body dynamics”；这个分解本身可能不唯一，结果由约束和数值正则共同确定。

#### 3. 第二阶段统一 whole-body QP 为什么还需要再求一次？

PADP 只把 locomotion 意图翻译成全身动力学变量，还没有完成操作任务。最终 QP 将 `x_PADP` 当作软先验，同时加入：

- **末端 6D 位姿/速度跟踪**：通过末端 Jacobian 将期望空间加速度写进全身 `q̈`，因此腿、底座和手臂都能帮助末端；
- **底座姿态与高度的轻度正则**，但允许操作任务使身体主动升降、倾斜或移动；
- **接触力分配正则**与机械臂 nominal posture，减少奇异位形和自碰风险；
- **末端 wrench tracking**；接触墙面时可沿法向控力、沿切向运动，即 hybrid force-motion control；
- 与 PADP 相同的动力学、接触、摩擦和执行器限制。

最终执行的是第二个 QP 求出的关节力矩，而不是 RL policy 的原始 PD action，也不是 `x_PADP` 直接下发。优化得到的底座加速度还会积分成速度命令，反馈给冻结的 locomotion policy。这构成双向协调：policy 给 QP 一个稳定行走先验，QP 又告诉 policy 下一步应该怎样移动底座来帮助操作。

### 它与几类常见方法的区别

| 方法 | 谁负责腿/底座 | 谁负责机械臂 | 主要问题或优势 |
|---|---|---|---|
| RL + Arm-QP | RL locomotion policy | 独立 Arm-QP | 模块简单，但手臂与底座缺少动力学级协调，远距离目标容易够不到。 |
| 端到端 whole-body RL | 一个统一策略 | 同一个策略 | 可学复杂协同，但位置、力、约束和新任务通常都进入训练设计，任务变化可能需要重训。 |
| Whole-body MPC/QP | 模型优化器 | 同一个模型优化器 | 约束明确、可解释，但对复杂地形的运动先验和计算量可能成为问题。 |
| **本文 PADP + unified QP** | 复用 RL 行走先验，再由 QP调整 | 统一 QP | 不重训 locomotion policy，且能显式加入末端位姿/力和物理约束；代价是依赖模型、接触估计和两层 QP。 |

因此，本文创新并不是“RL 学会了 whole-body manipulation”，而是**让已有 RL locomotion policy 能成为 model-based whole-body controller 的动力学级先验**。真正完成 manipulation 的是后面的 QP 目标与约束。

### 实验设置与结果

- **仿真**：MuJoCo，Unitree Go2 点足四足（15 kg）+ Unitree Z1 六自由度机械臂（4.5 kg），步长 2 ms。腿部 policy 预先在 Isaac Gym 训练。
- **真机**：DEEP Robotics LYNX M20 轮足四足（33 kg）+ Z1，板载 Intel i7-1360P；只有本体状态估计，没有视觉感知。足端接触来自足力传感器或动量接触观测器，状态估计采用 Kalman-filter pipeline。
- **运行频率**：policy 异步 50 Hz；PADP 与最终 whole-body QP 合计约 `0.85±0.25 ms`，以 500 Hz 运行。论文实现使用 Pinocchio 与 qpOASES。
- **仿真任务**：不同半径下随机末端目标的 workspace reachability；平地/不平地连续末端 tracking；对底座施加 70 N 或对末端施加 50 N、持续 0.4 s 的扰动。
- **定量结果**：平地上本文和 whole-body MPC 的 tracking 精度相近；不平地上本文的末端位置 RMSE 为 `0.011±0.013 m`，文中 MPC 为 `0.048±0.082 m`、whole-body RL 为 `0.034±0.048 m`。这支持“复用 locomotion prior 对不平地稳定性有帮助”，但不等于在所有任务上全面超过两类方法。
- **真机展示**：弹力带拉拽、擦墙、接触写字、抓取搬运与放下、开门、人工拖动下的顺应、人为末端轨迹，以及草地持物和上下台阶。操作目标/轨迹由操作者遥控输入，论文没有展示自主视觉识别与任务规划。

### 消融与证据边界：这篇论文最需要谨慎看的地方

论文的仿真对照包括 `RL + Arm-QP`、`PADP + Arm-QP`、本文统一 QP、whole-body MPC 和 whole-body RL。前两项能够说明：**只把 locomotion 和 arm controller 拼起来不够，统一全身优化确实扩大了 workspace 并改善协调**。真机也覆盖了位置、力、混合力位和不平地任务，工程完整度较高。

但核心方法仍有几处证据缺口：

1. **没有直接消融两阶段 PADP 的必要性**。作者讨论了一个更简单的方案：在最终 QP 中直接跟踪 raw policy torque，即单阶段求解；但实验没有把它作为 baseline。因此，论文验证了“本文完整系统能工作”，没有充分证明“先 PADP 再统一 QP”一定优于设计良好的单阶段 torque-prior QP。
2. **没有报告 `α_prior` 的灵敏度**。这是控制器在“保留 policy 稳定性”和“允许全身适应任务”之间的关键权重，但没有系统扫描。
3. **真机结果以展示为主**。没有给出各任务的成功次数、末端轨迹误差或接触力 RMSE；擦墙、写字、开门等照片能证明可行性，不能支持精确的统计比较。
4. **baseline 公平性信息有限**。whole-body MPC 在文中以 80 Hz 评估、本文 QP 为 500 Hz；whole-body RL 的训练任务、训练预算和泛化条件也没有在这篇 8 页论文中充分展开。Table I 更像跨论文能力清单，不是同一平台、同一协议下的全面性能排名。
5. **依赖显式模型与接触状态**。质量/惯量、Jacobians、足端接触模式和滚动接触运动学误差都会进入 QP。作者也明确承认，高度不规则、松软或可变形地面造成的接触点误差与快速接触切换可能降低性能。
6. **没有自主感知层**。末端目标、墙面接触方向和期望力均由上层给定；论文解决的是 task-space command 到 torque 的全身执行问题，不是“看到门把手后自主靠近、抓住并开门”。

### 对我们做 mani-loco 的启发

这篇与前面的 LeWM / PSG-JEPA 处在完全不同的层级：JEPA 工作研究怎样从视觉学习可用于规划的状态表征；PADP 研究给定明确末端目标后，怎样让腿、底座和机械臂在动力学层协同执行。两者可以形成上下层，而不是互相替代：

```text
视觉/世界模型或任务策略
        ↓  产生目标物体、末端轨迹或期望接触力
PADP 式 whole-body execution layer
        ↓
满足接触与动力学约束的全身关节力矩
```

对我们最有价值的是**动作接口设计**：高层感知策略不必直接搜索几十维关节力矩，可以输出低维的 base/末端 task-space command，由类似本文的全身层执行并保证接触与执行器约束。不过这也会把研究定位成分层式感知规划 + model-based execution，而不是完全端到端的 mani-loco policy。若想以本文为 baseline，最关键的实验应是加入自主视觉目标、动态/未知接触和模型误差，并补做 `raw torque prior single-stage QP` 与 PADP 两阶段版本的严格消融。

**我的判断**：这篇的贡献是一个有价值、工程上完整的**策略—动力学桥接层**。它比“腿 policy + 独立 arm controller”更统一，也比从头训练 whole-body RL 更容易复用已有 locomotion 能力；但论文标题里的 “whole-body dynamics” 不意味着 policy 学会或预测了全身动力学，真实含义是通过两个显式 QP 把 policy action 嵌入到全身动力学约束中。它适合作为 mani-loco 的低层执行基线，而不是感知、规划或 foundation model 方法。
